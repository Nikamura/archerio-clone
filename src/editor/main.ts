/**
 * Wall Pattern Editor - Entry Point
 */

import { EditorApp } from "./EditorApp";

// Initialize editor when DOM is ready
document.addEventListener("DOMContentLoaded", () => {
  new EditorApp();
});
